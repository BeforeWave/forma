# Development Overlay Rules

- Keep implementation choices aligned with existing repository conventions.
- Preserve unrelated user work while executing the selected plan.
- Treat validation commands as conditional on the recorded `Plan Type`.
