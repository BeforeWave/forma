- [x] [skeleton-and-methodology] Establish the Python project skeleton and author the canonical plan-first methodology source
Accept: Task Type=step; pyproject.toml at the repo root discovers both `forma` (from src/forma) and `forma_verifier` (from source/skill-creator/scripts), declares deps click/pyyaml/pytest, and exposes the `forma` console script; `.gitignore` covers the standard Python transient set; `src/forma/{__init__,cli}.py` exists with the `verify` / `create` subcommands wired to placeholder functions; `pip install -e .` succeeds; `source/methodology/{shape,gauge,seal,pour,flow}.md` and `source/methodology/references/{decision-gate,plan-strategy,task-structure,output-format}.md` contain the canonical plan-first rules (shape = chat convergence, gauge = read-only repo grounding, seal = lock plan files, pour = single-task execution with review gate, flow = autopilot)
Validate: pip install -e .
Validate: forma --help
Constraint: no Layer 1 bundle content, no verifier rules, no creator logic, no tests in this task; cli.py subcommands may exit 0 with a placeholder message

- [ ] [layer-1-and-verifier] Ship Layer 1 meta skill source with Layer 2 verifier organizationally inside it
Accept: Task Type=step; source/skill-creator/SKILL.md has strict Codex-compatible skill frontmatter (name: forma-creator, description only), progressive-disclosure load pointers, and instructs the agent to invoke scripts/verify.py before reporting success; Codex UI metadata lives in source/skill-creator/interfaces/codex/openai.yaml for adapter emission; source/skill-creator/references/*.md are hand-derived from source/methodology/references and are referenced by the SKILL.md; source/skill-creator/scripts/verify.py and source/skill-creator/scripts/forma_verifier/{rules,runner,report}.py implement structural + methodology rules using stdlib only; `forma verify` CLI wires through forma_verifier; tests/test_verifier.py covers each rule with paired valid + invalid fixtures under tests/fixtures/{valid-suite,invalid-suite}/; tests/test_layer_1_dogfood.py asserts the Layer 1 source passes verification
Validate: forma verify source/skill-creator/
Validate: python -m pytest tests/test_verifier.py tests/test_layer_1_dogfood.py
Use-Check: verify-self
Depends: skeleton-and-methodology
Constraint: source/skill-creator/scripts/forma_verifier must use stdlib only; no cross-skill borrowing; copying a target-specific forma-creator bundle alone (no other repo files, no pip install) must remain sufficient for an agent to generate and verify that target's plan-first suite

- [ ] [layer-3-creator-and-example] Implement Layer 3 creator, author the sample-org injection, and commit Layer 3's generated output as evidence
Accept: Task Type=gate; src/forma/creator/{manifest,composer,emitter}.py implement injection schema + composition + emission with the documented .forma-manifest.json provenance fields; `forma create --inject examples/injection/sample-org --output <dir>` produces a structurally valid, methodology-conformant skill suite; examples/injection/sample-org/injection.yaml is hand-authored with org/terminology, validation overrides, and optional Decision Gate extras; the committed examples/generated/sample-org-plan-first/ is regenerated from running the creator against the committed injection and includes .forma-manifest.json; tests/test_creator.py invokes the creator pipeline against examples/injection/sample-org/, asserts the manifest fields and that the result passes forma_verifier
Validate: python -m pytest tests/test_creator.py
Validate: forma verify examples/generated/sample-org-plan-first/
Use-Check: verify-generated
Depends: layer-1-and-verifier
Constraint: Layer 3 composes directly from source/methodology/ + injection layer and must not depend on the hand-authored source/skill-creator/ bundle; injection schema rejects unknown top-level keys; committed examples/generated/sample-org-plan-first/ must match what `forma create` produces from the committed inputs

- [ ] [docs-refresh] Update README.md and STRUCTURE.md to reflect the three layers as present
Accept: Task Type=step; README.md replaces the bootstrap status block with an Install / Verify / Generate walkthrough, shows the end-to-end snippet (uv run --extra dev forma verify source/skill-creator/, forma create --inject ... --output ..., forma verify <output>, forma build-creator --source source/skill-creator --output ... --target codex, and the same build command with --target claude-code), names the generated Layer 1 install paths (~/.codex/skills/forma-creator/, ~/.claude/skills/forma-creator/), and explicitly notes that Layer 2 ships inside the meta skill source and target contracts are injected into generated creator bundles; STRUCTURE.md moves source/, src/forma/, tests/, examples/ from "planned" to "present", and adds the Layer-2-inside-Layer-1 plus creator-builder organization notes
Validate: # no-programmatic-validation: docs-only
Depends: layer-3-creator-and-example
Constraint: no behavior or schema changes in this task; documentation only

- [ ] [target-aware-create-builder] Rework Layer 3 create into a strict target-specific plan-first bundle builder
Accept: Task Type=gate; `forma create` requires `--target codex|claude-code` and `--profile <file>` and fails closed when either is omitted or unknown; the generated Mode-S output is always exactly five plan-first skill directories (`shape/`, `gauge/`, `seal/`, `pour/`, `flow/`) plus `.forma-manifest.json`; the manifest records target, top-level profile, resolved profile order, methodology git short SHA, methodology tree digest, resolved profile file hashes, generator version, and a real current UTC timestamp; generated skill bodies and target metadata are deterministic from target + methodology + resolved profile, with `generated_at` as the only default time-varying field unless `FORMA_GENERATED_AT` is set; Codex output includes `agents/openai.yaml` inside every generated skill with `interface.display_name`, `interface.short_description`, `interface.default_prompt`, and `policy.allow_implicit_invocation`; Claude Code output does not emit Codex-only `agents/openai.yaml` or `interfaces/codex`; `forma verify` enforces these target rules whenever `.forma-manifest.json` records a target; the committed generated drift baselines are regenerated as `examples/generated/sample-backend-go-plan-first-codex/` and `examples/generated/sample-backend-go-plan-first-claude-code/`; tests cover both target outputs, omitted/invalid target failures, omitted/invalid profile failures, manifest target/profile/provenance, Codex metadata presence, Claude Code metadata absence, deterministic body emission from the same resolved profile, and verifier target-layout failures
Validate: uv run --extra dev forma create --target codex --profile examples/profiles/sample-backend/sample-backend-go.yaml --output /tmp/sample-backend-go-suite-codex
Validate: uv run --extra dev forma create --target claude-code --profile examples/profiles/sample-backend/sample-backend-go.yaml --output /tmp/sample-backend-go-suite-claude-code
Validate: uv run --extra dev forma verify /tmp/sample-backend-go-suite-codex
Validate: uv run --extra dev forma verify /tmp/sample-backend-go-suite-claude-code
Validate: uv run --extra dev forma verify examples/generated/sample-backend-go-plan-first-codex/
Validate: uv run --extra dev forma verify examples/generated/sample-backend-go-plan-first-claude-code/
Validate: uv run --extra dev pytest -p no:cacheprovider tests/test_creator.py tests/test_verifier.py
Depends: docs-refresh
Constraint: no target-neutral installable output; no runtime target switching from an installed creator; no Layer 3 chat-only ad hoc injection; Layer 3 composes from `source/methodology/` plus resolved profiles and must not depend on the hand-authored Layer 1 `source/skill-creator/` bundle

- [ ] [profile-resolver-hardening] Replace the weak injection overlay with a strict composable profile resolver
Accept: Task Type=step; `examples/profiles/sample-backend/sample-backend-go.yaml` is a sanitized top-level profile that includes other sanitized profiles such as `base.yaml`, `dev.yaml`, `backend.yaml`, and `languages/go.yaml`; each profile uses the v1 bundle-profile schema with allowed top-level keys `profile`, `includes`, `bundle`, `org`, `skills`, `terminology`, `validation_commands`, `decision_gate_extras`, and `constraints`; includes are resolved recursively before the local profile is applied; cycle detection, missing include rejection, and deterministic resolved-order recording are implemented; merge rules are explicit: scalar review fields are last-wins, lists append with deterministic de-duplication, and mappings deep-merge with later profiles overriding the same key; `bundle` may set review-facing name/description but cannot override the CLI target or rename required stage directories; `skills.<shape|gauge|seal|pour|flow>.description` may refine trigger descriptions while preserving each stage's methodology contract; terminology, validation commands, decision-gate extras, and constraints are additive local rules emitted into the relevant generated skill bodies; unknown top-level keys and unknown nested keys are rejected; arbitrary markdown/template injection is not supported; tests prove invalid profiles cannot void methodology or target contracts; docs or comments make clear that an agent may draft profiles but profile files only become durable source after human review and commit; no real downstream organization-specific profile content is committed to Forma examples
Validate: uv run --extra dev pytest -p no:cacheprovider tests/test_creator.py
Depends: target-aware-create-builder
Constraint: profiles control business specificity, domain/language constraints, validation commands, and display-level descriptions only; they must not become a second templating language or an escape hatch around Layer 2 rules; do not auto-track Layer 1 ad hoc constraints unless the user explicitly promotes them into a profile file

- [ ] [target-aware-docs-refresh] Refresh docs and issue evidence to match the corrected single-target creator and target-specific create model
Accept: Task Type=step; README.md and STRUCTURE.md show `forma create --target codex|claude-code --profile <file>`, the sanitized sample-backend composable profile stack, the two committed generated output directories, and `forma build-creator --target codex|claude-code`; docs explain that real downstream profiles with organization-specific workflow commands or constraints belong in their owning downstream repositories, not in Forma examples; docs explain that `forma-creator` is installed into one agent target and then generates plan-first five-skill bundles for that fixed target; docs explain the two authoring paths: Layer 1 can accept one-off ad hoc constraints during an agent interaction, verify the generated bundle with Layer 2, and install/handoff without tracking those constraints; Layer 3 resolves tracked profiles and emits target bundles in a repeatable, reviewable way; docs explain that selecting a top-level profile causes its included profiles' constraints to be resolved and emitted into the target bundle; docs remove target-neutral create and `--inject` examples and do not describe `adapt-creator`; issue notes mention that `build-creator` replaced the rejected `adapt-creator` wording and that `create` is a strict builder, not a loose demo emitter
Validate: uv run --extra dev forma --help
Validate: uv run --extra dev forma create --help
Validate: uv run --extra dev forma build-creator --help
Depends: profile-resolver-hardening
Constraint: documentation must describe the current implemented command names and target semantics, not the earlier abandoned adapter shape
