interface:
  display_name: "Forma Creator"
  short_description: "Create verified task-level Plan-First workflow outputs"
  default_prompt: "Use $forma-creator to create and verify a target-specific Plan-First workflow bundle or Codex plugin."
policy:
  allow_implicit_invocation: true
