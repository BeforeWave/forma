interface:
  display_name: "Forma Creator"
  short_description: "Create verified workflow bundles"
  default_prompt: "Use $forma-creator to create a verified Plan-First workflow bundle."
policy:
  allow_implicit_invocation: true
